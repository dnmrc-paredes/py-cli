[project]
name = "py_cli"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "dnmrcprds", email = "dnmrcprds@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "httpx>=0.28.1",
    "questionary>=2.1.1",
]

[project.scripts]
py_cli = "py_cli:main"

[build-system]
requires = ["uv_build>=0.12.12,<0.13.0"]
build-backend = "uv_build"
